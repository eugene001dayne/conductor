name: echo-test
version: "1.0"

steps:
  - id: step-1
    type: echo
    params:
      message: "Hello from CONDUCTOR. Phase 1 is alive."

  - id: step-2
    type: echo
    params:
      message: "Workflow engine is connected to Temporal Cloud."

  - id: step-3
    type: echo
    params:
      message: "YAML parsed. Activities executed. We are ready to build."